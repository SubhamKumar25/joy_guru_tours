const adminOnly = (req, res, next) => {
  if (req.user && req.user.role === 'admin') {
    next();
  } else {
    res.status(403);
    return next(new Error('Access forbidden, administrator privileges required'));
  }
};

module.exports = { adminOnly };
