# Joy Guru Travel Platform

> Exported from UXMagic Copilot on 7/11/2026, 1:54:12 PM

## Quick Start

```bash
npm install
npm run dev   # or: npm start
```

Open any of the HTML files directly in a browser — no build step needed.

## File Structure

```
Joy Guru Travel Platform/
 ├── Landing Page.html
 ├── Search Results.html
 ├── Tour Packages.html
 ├── Booking & Payment.html
 ├── User Dashboard & Invoice.html
 ├── Admin Control Center.html
 ├── styles.css
 ├── script.js
 ├── package.json
 ├── .cursorrules
 ├── README.md
```

## Pages

- **Landing Page.html** — Landing Page
- **Search Results.html** — Search Results
- **Tour Packages.html** — Tour Packages
- **Booking & Payment.html** — Booking & Payment
- **User Dashboard & Invoice.html** — User Dashboard & Invoice
- **Admin Control Center.html** — Admin Control Center

## How Styles Work

- Each HTML page has an inline `<style type="text/tailwindcss">` block containing
  the `@theme inline` mapping and `:root` design tokens. This is required by the
  Tailwind v4 browser runtime to resolve utility classes like `bg-background`.
- `styles.css` contains the shared `:root` variable blocks (clustered by theme)
  plus any page-specific non-Tailwind CSS scoped to `[data-page="..."]`.

## How Scripts Work

`script.js` contains shared utilities and wraps each page's inline scripts in
named IIFEs to prevent variable collisions across pages.

---
*Generated by UXMagic Copilot*
